#include <bits/stdc++.h>
using namespace std;
// good problem: Find the maximum median of any subarray with length at least K
/*
median: in case of odd elements median is just the middle element of the sorted
order in case of even elements, median is the minimum of the two middle elements
in sorted order
*/
int main() { return 0; }